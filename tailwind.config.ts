import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: "#6F8FAF",
        secondary: "#C7D3DD",
        accent: "#B9A5D8",
        background: "#FAFAFA",
        text: "#333333",
      },
    },
  },
};

export default config;
